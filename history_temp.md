  
### dovecot-pop3d install

  ```bash
  sudo apt-get update
  sudo apt-get upgrade
  sudo apt-get -y install dovecot-pop3d
  protocols = pop3
  mail_location = maildir:~/Maildir
  /etc/dovecot/
  cd /etc/dovecot/
  ls
  nano dovecot.conf 
  sudo nano dovecot.conf 
  sudo systemctl start dovecot
  sudo systemctl enable dovecot
```

### 

```bash
  sudo ufw status verbose
  telnet
  [200~sudo apt-get install postfix
  ~sudo apt-get install postfix
  sudo apt-get install postfix
  cd ~
  ls -la
  sudo ufw status
  ss
  ss | grep --color=always -E "25"
  ss | grep --color=always -E "mail"
  ss -a  | grep --color=always -E "mail"
  sudo apt-get install mailutils
  echo "This is the body of the email" | mail -s "This is the subject line" t995771@gmail.com
  mail -s "This is the subject line" t995771@gmail.com
  ls -la
  sudo nano /etc/postfix/main.cf
  ~
  cd ~
  ls
  md Maildir
  mkdir Maildir
  ls
  sudo nano /etc/dovecot/dovecot.conf 
  reboot
  sudo reboot
  cd /etc/
  ls -la
  ps
  ps -a
  sudo systemctl start dovecot
  cd /dev/
  ls
  less -RS 
  cd log
  cd ..
  ls
  cd ver
  cd var
  ls
  cd mail
  ls
  cat t9957
  cd ..
  ls
  ls -a
  cd log
  ls
  less < auth.log
  ls -a
  less mail.log 
  cd ~
  mail -f ~/Maildir
  ls
  cd Maildir/
  ls -la
  ls 
  ls -la
  cd ..
  ls -la
  rm Maildir/
  rm -d Maildir/
  ls
  sudo systemctl start dovecot
  mail -f ~/Maildir
  exit
  echo "This is the body of the email" | mail -s "This is the subject line" t9957@192.168.1.131
  mail -f ~/Maildir
  echo "This is the body of the email" | mail -s "This is the subject line" t9957@g05p001
  mail -f ~/Maildir
  sudo systemctl start dovecot
  sudo systemctl status dovecot.service
  sudo nano /etc/dovecot/dovecot.conf 
  cat /etc/dovecot/dovecot.conf 
  sudo journalctl -xeu dovecot.service
  sudo nano /etc/dovecot/dovecot.conf 
  sudo systemctl status dovecot.service
  sudo systemctl start dovecot
  sudo systemctl enable dovecot
  echo "This is the body of the email" | mail -s "This is the subject line" t9957@g05p001
  mail -f ~/Maildir
  sudo systemctl restart postfix
  mail -f ~/Maildir
  echo "This is the body of the email" | mail -s "This is the subject line" t9957@g05p001
  mail -f ~/Maildir
  sudo tail /var/log/mail.log
  mkdir -p ~/Maildir/{cur,new,tmp}
  chmod -R 700 ~/Maildir
  ls
  ls -a
  rm Maildir 
  mkdir -p ~/Maildir/{cur,new,tmp}
  chmod -R 700 ~/Maildir
  echo "This is the body of the email" | mail -s "This is the subject line" t9957@g05p001
  mail -f ~/Maildir
  echo "This is the body of the email" | mail -s "This is the subject line" t995771@gmail.com
  history